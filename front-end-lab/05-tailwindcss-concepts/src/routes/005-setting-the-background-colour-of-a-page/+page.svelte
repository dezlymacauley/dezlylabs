<!-- 
  `min-h-screen` gives the div a height so that the `bg-purple-700` colour
  is visible.

  `min-h-screen` also ensures that the height is the div is never 
  shorter than the height of the the screen that the page is being view on.

  In simple terms, the background colour will take up the entire screen.
-->
<div class="bg-purple-700 min-h-screen">

</div>
