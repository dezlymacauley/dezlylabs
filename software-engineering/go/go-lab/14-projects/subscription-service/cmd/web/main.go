package main

const webPort string = "80"

func main() {
    // Connnect to the database

    // Create sessions (All the rendering of pages will be done server side)

    // Create channels

    // Create wait group for those channels

    // Set up the application configuration

    // Set up email. Send an email to a user once they have subscribed for
    // the service. This needs to be concurrent

    // Listen for web connections
}
