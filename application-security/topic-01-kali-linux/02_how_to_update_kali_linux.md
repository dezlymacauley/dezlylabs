Update the package manager
```sh
sudo apt update
```

This command installs the newest versions of all installed packages, 
and if needed, it can remove or replace packages to complete the upgrade.
```sh
sudo apt full-upgrade
```
Removes packages that were automatically installed as dependencies but 
are no longer needed.
```sh
sudo apt autoremove
```
